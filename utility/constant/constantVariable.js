const QUEUE_NAME = {
  EMAIL_QUEUE: 'emailQueue',
};

const JOB_NAME = {
  SEND_EMAIL: 'sendEmail',
};

const ROLES = {
  ADMIN: 'admin',
  CLIENT: 'client',
};

const TIME_UNIT = {
  DAY: 'days',
  HOUR: 'hour',
  MINUTE: 'minutes',
  SECOND: 'seconds',
};

module.exports = {
  QUEUE_NAME,
  JOB_NAME,
  ROLES,
  TIME_UNIT,
};
